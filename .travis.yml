language: julia

os:
  - linux
  - osx
julia:
  - 1.3
  - 1.4
  - nightly
env:
  - JULIA_NUM_THREADS=1
  - JULIA_NUM_THREADS=4

notifications:
  email: false

jobs:
  allow_failures:
    - julia: nightly
